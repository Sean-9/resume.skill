# profile.md 字段规范

`./jobsearch/profile.md` 是 resume.skill 的唯一真源，也是 interview.skill 的输入。

**核心约定**：这个文件存放**全量经历，不做取舍、不做润色**。取舍发生在 Phase 4，润色发生在 Phase 5，都不回写这个文件。同一份经历库可以生成任意多份定向简历。

---

## 一、文件结构

```
# 经历库
owner: {姓名}
updated: {YYYY-MM-DD}
target_roles: [产品经理, 解决方案]     # 可选，用于取舍时的默认偏好

## EXP-01
{经历块}

## EXP-02
{经历块}
```

---

## 二、经历块字段

| 字段 | 说明 | 必填 |
|---|---|---|
| `id` | `EXP-01`，两位序号，按录入顺序，**不可重排、不可复用** | ✓ |
| `type` | 工作 / 实习 / 项目 / 科研 / 竞赛 / 社团 / 自学 / 兼职 | ✓ |
| `org` | 组织名。学生项目写学校+课程/实验室 | ✓ |
| `role` | 你在里面的角色 | ✓ |
| `period` | `2024.03 - 2025.06`，至今写 `- 至今` | ✓ |
| `context` | 当时业务/团队处在什么状态，为什么要做这件事 | ✓ |
| `items[]` | 素材条目，见第三节 | ✓ |
| `tags` | 技能标签，自动生成，供 JD 匹配 | 自动 |
| `raw` | 用户原话/原材料存档，**一字不改** | ✓ |

`raw` 字段是证据闸门的比对基准，任何时候不得改写或"整理"。

---

## 三、items[] 字段

| 字段 | 说明 | 必填 |
|---|---|---|
| `id` | `EXP-01-2`，块 ID + 条目序号 | ✓ |
| `action` | 我具体做了什么。**强制第一人称，"我们"必须拆开** | ✓ |
| `decision` | 卡在哪、怎么选的、为什么不选另一条路 | 允许空，但空值影响动词权限 |
| `result` | 做完之后什么变了 | ✓ |
| `metric` | 数字或代理指标 | 允许空 |
| `metric_source` | 证据等级，见第四节 | ✓ |

### decision 为空的后果

Phase 5 改写时，**decision 为空的素材不得使用"主导/设计/搭建"类动词**，只能用"参与/完成/实现"。这不是措辞偏好，是防止把执行者包装成决策者后在面试第 2 层追问翻车。

---

## 四、证据等级 metric_source

| 等级 | 定义 | 简历里允许怎么写 | 压测处理 |
|---|---|---|---|
| `verified` | 有数字，且用户说得清怎么测的、基线是什么 | 直接写数字 | 常规 4 层追问 |
| `claimed` | 有数字，说不清测量方式 | 可写 | **强制标红**，优先追问来源 |
| `proxy` | 无数字，有代理指标（覆盖人数、频次、前后时长对比） | 只能写代理指标本身 | 常规追问 |
| `none` | 什么都没有 | **禁止出现任何数字** | 常规追问 |

### 三条不可违反的规则

1. 模型**不得**把 `proxy` 升格为 `claimed`。"快很多" 不能变成 "提升 10 倍"。
2. 模型**不得**把 `claimed` 升格为 `verified`。用户报了数字不等于数字站得住。
3. 降级随时可以，升级只能由用户补充信息后手动触发。

### 判定示例

| 用户原话 | 等级 | 理由 |
|---|---|---|
| "从 2 天降到 10 分钟，我两次都记了时间" | verified | 有基线，有测量 |
| "效率提升了大概 30% 吧" | claimed | 有数字，来源不明 |
| "组里 6 个人都在用" | proxy | 无效率数字，但覆盖范围是硬事实 |
| "后来快很多了" | none | 什么都没有 |

---

## 五、完整样例

```markdown
## EXP-03
type: 科研
org: XX大学 分析化学实验室
role: 研究生 / 数据处理
period: 2024.03 - 2025.06
context: 组内 6 人共用一台色谱仪，每次出数据后手工整理成周报，
         整理占掉每人每周约半天，且格式不统一，导师无法做跨批次横向对比
tags: [Python, pandas, 数据清洗, 流程自动化, 需求抽象]

items:
  - id: EXP-03-1
    action: 我用 Python + pandas 写了脚本，把仪器导出的 csv 自动清洗、
            合并、按导师要求的模板生成周报
    decision: 一开始想做 Excel 宏，因为组里只有我会 Python。后来选了
              Python 打包成 exe，理由是宏在不同 Office 版本下经常挂，
              exe 双击就能跑，别人不用装环境
    result: 组内 6 人都在用，导师开始能做跨批次对比
    metric: 组内 6 人使用；单人每周约省半天
    metric_source: proxy

  - id: EXP-03-2
    action: 我设计了字段映射配置文件，仪器换型号时改配置不用改代码
    decision: 组里换过一次仪器，第一版脚本全废了重写。之后才加的这层，
              代价是首次配置比硬编码多花半天
    result: 后来又换过一次仪器，改配置 10 分钟搞定
    metric: 换仪器适配从约 2 天（重写脚本）降到 10 分钟
    metric_source: verified

raw: |
  就是帮实验室写了个脚本处理数据，之前手工整理，后来快很多。
  中间换过仪器，第一次全重写了，后来搞了个配置文件，第二次换就很快。
  组里都在用。
```

同一段经历里两条素材的证据等级不同，Phase 5 写出来的措辞就不同：

- `EXP-03-2`（verified + decision 非空）→ 「**设计**配置化字段映射方案，仪器适配周期从 2 天压缩至 10 分钟」
- `EXP-03-1`（proxy + decision 非空）→ 「**搭建**实验数据自动化处理流程，覆盖组内 6 人日常周报产出」——注意没有百分比、没有倍数

---

## 六、校验规则（写入前必跑）

| 检查 | 失败处理 |
|---|---|
| id 唯一且未被复用 | 报错，重新分配 |
| action 含第一人称，且不含未拆分的"我们" | 打回追问 |
| result 非空 | 标 `[需你补充]` |
| metric 非空时 metric_source 必须非空 | 打回判定 |
| raw 非空 | 拒绝写入 |
| metric 中的数字能在 raw 里找到对应表述 | 降级或剥离数字 |

---

## 七、给 interview.skill 的读取说明

interview.skill 读取本文件时：

1. 用 `context` + `action` + `decision` 生成行为面题（STAR 类）
2. **优先针对 `metric_source: claimed` 的素材出题**——这些是用户简历上写了数字但说不清来源的地方，真面试最容易翻车
3. `decision` 为空的素材，出题方向改为"这件事你有没有做过判断"，考察是否只是执行者
4. `tags` 用于校准技术领域轮的深度
